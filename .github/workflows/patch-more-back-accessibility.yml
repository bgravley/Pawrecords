name: Patch More Back Accessibility

"on":
  push:
    branches: [fix/accessibility-more-back-label]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: fix/accessibility-more-back-label

      - name: Label More back button
        run: python3 scripts/patch_more_back_accessibility.py

      - name: Commit fix
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/PawRecord.jsx
          git diff --cached --quiet && exit 0
          git commit -m "Label More back button for assistive technology"
          git push origin HEAD:fix/accessibility-more-back-label
